server:
  port: ${PORT:8077}
rabbitmq:
  management-url: ${RABBITMQ_URL:http://localhost:15672}
  username: ${USERNAME}
  password: ${PASSWORD}
  definitions-file: ${DEFINITIONS_FILE:definitions.json}

logging:
  level:
    root: INFO